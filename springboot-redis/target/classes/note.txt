redis基于setnx实现分布式锁
执行setNx命令时，如果该key不存在时，结果返回1 ，RedisTemplate返回true。如果该key存在时，结果返回0,RedisTemplate返回false

redis实现分布式锁如何避免死锁问题？
1、给key设置过期时间（会出现续命问题：业务执行时间大于过期key）
2、开启一个定时任务实现续命，当业务逻辑没执行完毕的时候，需要延长过期key的时间
3、设置续命的次数，续命多次还没执行完业务逻辑的情况下，就应该回滚业务，再去主动释放锁